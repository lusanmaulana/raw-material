name: raw_material
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  assets_audio_player: ^3.1.1
  cupertino_icons: ^1.0.6
  file_picker: ^6.1.1
  file_saver:
    path: local_packages/file_saver
  mobile_scanner: ^3.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/audios/
